# Release: push a v* tag to publish a DMG.
#
#   Stable:      git tag -a v0.2.0 -m "TetherKit 0.2.0"        && git push origin v0.2.0
#   Prerelease:  git tag -a v0.2.0-beta.1 -m "TetherKit 0.2.0 beta 1" && git push origin v0.2.0-beta.1
#
# A tag with a suffix after the version (v0.2.0-beta.1, v0.2.0-rc.1) publishes a
# GitHub *prerelease*. Prereleases may be built without signing secrets: the
# DMG is then ad-hoc signed and not notarized, and only the DMG is uploaded.
#
# Stable tags require these repository secrets and refuse to publish without
# them (an unsigned DMG is blocked by Gatekeeper on every user's Mac):
#   MACOS_CERTIFICATE_P12        base64 of "Developer ID Application" cert + key (.p12)
#   MACOS_CERTIFICATE_PASSWORD   .p12 export password
#   NOTARY_KEY_P8                base64 of an App Store Connect API key (.p8)
#   NOTARY_KEY_ID                that key's ID
#   NOTARY_ISSUER_ID             App Store Connect issuer ID

name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    name: Build and publish ${{ github.ref_name }}
    runs-on: macos-26
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Classify tag; version must match CMakeLists VERSION
        id: tag
        run: |
          tag_ver="${GITHUB_REF_NAME#v}"
          base_ver="${tag_ver%%-*}"
          cmake_ver=$(sed -n 's/^  VERSION \([0-9.]*\)$/\1/p' CMakeLists.txt | head -1)
          echo "tag=${tag_ver} base=${base_ver} CMakeLists.txt=${cmake_ver}"
          [ "$base_ver" = "$cmake_ver" ] || { echo "::error::tag and VERSION differ"; exit 1; }
          if [ "$tag_ver" != "$base_ver" ]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Signing secrets
        id: signing
        env:
          HAS_CERT: ${{ secrets.MACOS_CERTIFICATE_P12 != '' }}
          HAS_NOTARY: ${{ secrets.NOTARY_KEY_P8 != '' && secrets.NOTARY_KEY_ID != '' && secrets.NOTARY_ISSUER_ID != '' }}
          PRERELEASE: ${{ steps.tag.outputs.prerelease }}
        run: |
          if [ "$HAS_CERT" = true ] && [ "$HAS_NOTARY" = true ]; then
            echo "signed=true" >> "$GITHUB_OUTPUT"
          elif [ "$PRERELEASE" = true ]; then
            echo "::warning::No signing secrets: publishing an ad-hoc signed, un-notarized prerelease"
            echo "signed=false" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Stable releases need MACOS_CERTIFICATE_P12 and the NOTARY_* secrets"
            exit 1
          fi

      - name: Select the newest Xcode
        run: |
          latest="$(ls -d /Applications/Xcode_*.app | sort -V | tail -1)"
          sudo xcode-select -s "${latest}"
          xcodebuild -version

      - name: Import signing certificate
        if: steps.signing.outputs.signed == 'true'
        env:
          MACOS_CERTIFICATE_P12: ${{ secrets.MACOS_CERTIFICATE_P12 }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          NOTARY_KEY_P8: ${{ secrets.NOTARY_KEY_P8 }}
        run: ./scripts/ci-import-certificate.sh

      - name: Build, test, package
        env:
          TETHERKIT_BUILD_NUMBER: ${{ github.run_number }}
          NOTARY_KEY_ID: ${{ steps.signing.outputs.signed == 'true' && secrets.NOTARY_KEY_ID || '' }}
          NOTARY_ISSUER_ID: ${{ steps.signing.outputs.signed == 'true' && secrets.NOTARY_ISSUER_ID || '' }}
        run: ./scripts/build-release.sh

      - name: Swift unit tests
        run: TETHERKIT_LIB_DIR="$PWD/build-release/lib" swift test --package-path gui

      - name: Release notes
        env:
          SIGNED: ${{ steps.signing.outputs.signed }}
        run: |
          dmg="$(ls dist/TetherKit-*.dmg)"
          {
            if [ "$SIGNED" != true ]; then
              echo '> [!WARNING]'
              echo '> **Test build — not signed with a Developer ID and not notarized.**'
              echo '> macOS will block it on first open. After copying TetherKit to Applications, either'
              echo '> open *System Settings › Privacy & Security* and click **Open Anyway**, or run'
              echo '> `xattr -dr com.apple.quarantine /Applications/TetherKit.app`.'
              echo '> The background component may refuse to register until the app is signed with a'
              echo '> real certificate; `sudo /Applications/TetherKit.app/Contents/MacOS/tetherkit-cli`'
              echo '> works regardless.'
              echo
            fi
            echo '## Install'
            echo
            echo "1. Download **$(basename "$dmg")**, open it and drag **TetherKit** to **Applications**."
            echo '2. Open TetherKit from Applications and click **Enable Background Component**.'
            echo '   macOS asks you to allow it once in *System Settings › General › Login Items*.'
            echo '3. Optional: *Settings › Command-line tool › Install Command* puts `tetherkit-cli`'
            echo '   on your PATH (`/usr/local/bin`).'
            echo
            echo 'Universal build: Apple Silicon and Intel, macOS 14+ (the bundled CLI runs on 13.3+).'
            if [ "$SIGNED" = true ]; then
              echo 'Signed with Developer ID and notarized by Apple.'
            fi
            echo
            echo '## Checksum (SHA-256)'
            echo
            echo "\`$(shasum -a 256 "$dmg" | awk '{print $1}')\`  $(basename "$dmg")"
          } > notes.md
          cat notes.md

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          PRERELEASE: ${{ steps.tag.outputs.prerelease }}
        run: |
          assets=(dist/TetherKit-*.dmg)
          flags=()
          if [ "$PRERELEASE" = true ]; then
            flags+=(--prerelease)
          else
            assets+=(dist/tetherkit-cli-*.tar.gz)
          fi
          gh release create "${GITHUB_REF_NAME}" \
            --title "TetherKit ${GITHUB_REF_NAME}" \
            --notes-file notes.md --generate-notes --verify-tag \
            "${flags[@]}" "${assets[@]}"
